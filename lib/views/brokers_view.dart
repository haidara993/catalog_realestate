import 'package:flutter/material.dart';

class BrokerView extends StatelessWidget {
  const BrokerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
